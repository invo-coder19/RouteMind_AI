"""
app/providers/openrouter.py
────────────────────────────
OpenRouter provider implementation.

STATUS: STUB — Implementation pending (Phase 1, Step 2).

This file will implement:
    - OpenRouterProvider(LLMProvider)
    - Async HTTP calls to OpenRouter's OpenAI-compatible API.
    - Request formatting using the OpenAI message format.
    - Response parsing and normalization into LLMResponse.
    - Token extraction from the 'usage' response field.
    - Error handling for auth, rate limit, timeout, and unavailability.
    - Cost calculation via CostCalculator using ModelConfig pricing.
"""

# Implementation will be added in the next Phase 1 step.
