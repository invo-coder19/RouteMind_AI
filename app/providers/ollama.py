"""
app/providers/ollama.py
────────────────────────
Ollama local provider implementation.

STATUS: STUB — Implementation pending (Phase 1, Step 2).

This file will implement:
    - OllamaProvider(LLMProvider)
    - Async HTTP calls to the local Ollama REST API (/api/chat).
    - Request formatting for Ollama's native format.
    - Response normalization into LLMResponse.
    - Token extraction from Ollama's prompt_eval_count / eval_count fields.
    - Error handling when Ollama is not running or model is not pulled.
    - Cost will be $0.00 for local inference (from ModelConfig pricing).
"""

# Implementation will be added in the next Phase 1 step.
