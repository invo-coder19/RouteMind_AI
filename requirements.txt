# --- Core runtime (Phase 1) --------------------------------------------------
httpx==0.27.0                  # Async HTTP client for all provider calls
# pydantic 2.11.7 → pydantic-core 2.33.2 ships cp313-cp313-win_amd64 binary wheel.
# Required by deepeval 4.2.2 which needs pydantic>=2.11.7.
pydantic==2.11.7               # Data validation (v2); pydantic-core 2.33.2
python-dotenv==1.1.1           # Load .env into os.environ (deepeval requires >=1.1.1)

# --- Retry / resilience (Phase 1) --------------------------------------------
tenacity==8.3.0                # Exponential backoff on HTTP 429 / 503

# --- Configuration parsing (Phase 2) ----------------------------------------
PyYAML==6.0.2                  # Parse config/routing_map.yaml in classifier/predict.py

# --- Data & ML (Phase 2 — pinned now for reproducibility) -------------------
# pandas 2.2.3 ships cp313-cp313-win_amd64 wheels — no source build needed.
# numpy resolved automatically by pandas/scikit-learn; both ship cp313 wheels.
pandas==2.2.3                  # DataFrame → baseline_results.csv
scikit-learn==1.5.2            # Complexity classifier baseline
lightgbm==4.3.0                # Upgraded classifier (ships py3-none-win_amd64)

# --- Evaluation (Phase 3) ----------------------------------------------------
# deepeval 4.2.2 ships py3-none-any; requires tenacity>=8.0.0,<=10.0.0 (compatible).
deepeval==4.2.2                # LLM evaluation framework (Phase 3)

# --- Async SQLite (Phase 3) --------------------------------------------------
aiosqlite==0.20.0              # Non-blocking SQLite for verification/feedback_store.py

# streamlit 1.45.0+ supports numpy 2.x (1.35.0 capped at numpy<2.0, preventing binary wheel use).
streamlit==1.45.0
plotly==5.24.1

# --- API server (Phase 5) ----------------------------------------------------
fastapi==0.111.0
uvicorn[standard]==0.30.1

# --- Database (Phase 4) -------------------------------------------------------
# asyncpg 0.30.0 ships cp313-cp313-win_amd64 wheels (0.29.0 did not).
asyncpg==0.30.0                # Async Postgres driver
sqlalchemy==2.0.30             # ORM / query builder (async mode)
alembic==1.13.1                # DB migrations

# --- Testing -----------------------------------------------------------------
pytest==8.2.1
pytest-asyncio==0.24.0